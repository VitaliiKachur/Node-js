"use client";

import Alert from "react-bootstrap/Alert";

export function LibraryNotice() {
  return (
    <Alert className="mb-0" variant="info">
      This block uses a React Bootstrap component.
    </Alert>
  );
}
